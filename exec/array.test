function print_arr_at_index returns nothing (arr as array, index as i64) {
	print(arr.get(index));
}

function main returns nothing () {
	arr = create array of size b(X)4 as i64;
	arr.push(b(X)1);
	arr.push(b(X)2);
	call print_arr_at_index(arr, b(X)0);
	call print_arr_at_index(arr, b(X)2);
	call print_arr_at_index(arr, b(X)3);
}
